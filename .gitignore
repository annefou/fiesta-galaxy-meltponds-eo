__pycache__/
*.pyc
.DS_Store
*.tmp
venv/
.venv/

# pixi environment (rebuilt from pixi.lock)
.pixi/

# snakemake bookkeeping
.snakemake/

# downloaded / derived data — regenerated by the notebooks (see DOMAIN.md:
# the repo ships without input data; notebooks/01 fetches it from AWS)
data/raw/
data/processed/

# large regenerated intermediate (the label raster is rebuilt by notebooks/03)
results/labels.tif

# jupytext-paired notebooks (the .py percent files are the source of truth)
notebooks/*.ipynb
